import { FilterColumnsByTeamPipe } from './filter-columns-by-team.pipe';

describe('FilterColumnsByTeamPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterColumnsByTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
